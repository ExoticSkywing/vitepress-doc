
*   微信版本`8.0.54`、`8.0.55`
*   主题盒子`1.0.5`以上

  

> 其他微信版本`未做适配`，部分图标显示错误和丢失属正常现象。  
> （不考虑向下兼容，请自行适配优化，如你愿意分享针对`31、33`版本的适配包，可以PR或私信提交分享，你将成为本项目的贡献者）

### 高频高优先级问题

如果你是通过其他作者的`原生主题`加载DIY素材，文件夹你拷对了吗？
---------------------------------------

 ![](https://api.minio.1yo.cc/nebuluxe/halosparkpix/%E6%8B%B7%E8%B4%9D%E6%96%87%E4%BB%B6%E5%A4%B9%E6%93%8D%E4%BD%9C.gif) 

必须设置项
---------------------------------------

重启微信以生效。（不要直接拷贝 `WeChat-iMessage-Themebox` 作为顶层文件夹）

若盒子没有自动创建 `diy` 文件夹，请先在 `Themebox` 中 `主题选项` - `素材加载模式` 中勾选 `自定义优先`，重启微信。这将触发盒子主动创建主题的 `diy` 子文件夹。再按照上述说明操作即可。

气泡形状参差不齐
---------------------------------------

![](https://api.minio.1yo.cc/nebuluxe/halosparkpix/image-83nk.png)

> 更新：
> 
> *   `Themebox` 1.0.9 版本已经更新，`主题选项` 单独增加了 `气泡裁剪` 配置项。开启气泡裁剪设置`18*18*18*18`即可。

关于气泡黑暗白天模式切换的字体颜色问题
---------------------------------------

> 修改：
> 
> *   通过`Themebox`兑换码兑换本主题的已默认调整好红包转账气泡封面字体颜色可见性,可定义`对方发送亮色、深色`颜色。
> *   通过`其他作者主题`加载DIY素材方式自定义`对方发送亮色、深色`颜色，红包转账封面字体会有可见性问题。

如果是第一次修改配色，请不要设置 \`对方发送亮色、深色\` 的颜色！  
如果是第一次修改配色，请不要设置 \`对方发送亮色、深色\` 的颜色！  
如果是第一次修改配色，请不要设置 \`对方发送亮色、深色\` 的颜色！

不设置将 \`保持微信默认\` 的对方字体颜色，这有助于在浅色模式下显示黑色，深色模式下显示白色的红包、转账气泡封面字体的 \`动态变化\`。如果设置了，可能导致红包转账封面的字体颜色 \`可见性变差\`。

> 如果设置了怎么办？ 目前 `Themebox` 还不能单独针对某方消息字体颜色设置进行开关启停。也不能清除已选择的颜色选择以恢复默认。 如需要恢复 `单方颜色`的设置，如：对方消息字体颜色，请重置插件。（操作按钮位于插件最底部位置）

* * *

  

```
- `修改消息颜色` 启用。
- `我发送的亮色` 设置为 `#ffffff`。
- `我发送的深色` 设置为 `#ffffff`

其他保持关闭。 
```

该部分可自由设置。

2.1.通过上线版`兑换码`兑换本主题的安装方法（推荐）
----------------------------

[](#21通过上线版兑换码兑换本主题的安装方法推荐)

> 使用`Themebox`兑换码兑换的请直接通过`Themebox`\-右上方的`空投箱`下载,下载后启用本主题即可，详细使用配置请参照第四章节。 已有兑换码还未兑换？前往**文章底部小礼物**进行兑换。

![](https://api.minio.1yo.cc/nebuluxe/halosparkpix/image-n0nx.png)

通过此方法可以直接通过主题插件的主题更新提示`直接更新`，无需手动下载主题包。 但如需使用提供的`自定义替换素材`，仍需要手动前往仓库下载主题包解压后将`自定义替换文件夹`内你所需要替换的素材导入到`DIY`文件夹)

（注意：`上线版`唯一好处只用于快速更新，再`无其他区别`。）

 ![](https://api.minio.1yo.cc/nebuluxe/halosparkpix/image-u9wh.png)   

> 如何获得兑换码？
> 
> *   参与每次版本更新发起的`抽奖活动`。
> *   关注`限量抢码`活动。
> *   参与本项目或衍生项目-（主题适配经验指南）做出贡献者赠送`3`份兑换码。  
>     （兑换码`不售卖`。即使没有兑换码你也仍然可以通过加载原生主题DIY素材的方式使用本主题）

2.2.通过其他作者的`原生主题`加载DIY素材安装的方法
-----------------------------

[](#22通过其他作者的原生主题加载diy素材安装的方法)

> 注意：通过`Themebox`兑换码兑换本主题的可直接通过主题插件下载。无需购买其他作者的`测试原生主题` 或 `官方主题`主题。

         下载[最新主题文件](https://github.com/AidenYang1/iMessageApp_WeChat_For_Themebox/releases)压缩包（里面包含主题文件夹和可供替换的红包转账气泡）。

1.  确保你已经激活 Themebox 。
2.  将解压后的主题文件夹“`WeChat-iMessage-Themebox`”内的所有文件和文件夹全选后（不要直接拷贝文件夹）导入你的`微信文件夹`中的“`themebox`\-`diy`\-`官方主题文件夹（一定要是原生主题）`”里。
3.  建议前往 `微信` - `设置` - `通用` - `字体大小` 将字体大小设置为 `标准以下`。
4.  使用插件开启`全局圆角！全局圆角！全局圆角！！！`否则图标尺寸显示不佳。（所有适配基于圆角模式下优化）。
5.  设置完成后，重启微信，以使主题生效。

将解压后的子文件夹中的`WeChat-iMessage-Themebox`主题文件夹下的`所有文件和文件夹`全选拷贝到你的主题盒子`测试原生主题`的 diy 文件夹中。

如：

```
你的iPhone/Documents/微信/ThemeBox/diy/测试原生主题 
```

重启微信以生效。（不要直接拷贝 `WeChat-iMessage-Themebox` 作为顶层文件夹）

若盒子没有自动创建 `diy` 文件夹，请先在 `Themebox` 中 `主题选项` - `素材加载模式` 中勾选 `自定义优先`，重启微信。这将触发盒子主动创建主题的 `diy` 子文件夹。再按照上述说明操作即可。

*   不要同时使用其他主题插件设置主题，否则会出现主题重叠、混乱、素材交替问题。
*   不要使用其他插件过分修改布局。

如果出现素材加载丢失、仍有之前主题的样式素材，请检查确保：

1.  使用的是兼容的微信版本`8.0.54`、`8.0.55`。
2.  `Themebox` 使用的是 `测试原生官方主题`或本主题`上线版`，没有错误启用其他主题。
3.  没有使用 `其他主题插件` 启用其他 `影响该主题素材加载` 的功能和布局。
4.  没有使用 `其他主题插件` 启用`其他主题`。

为获得最好的显示效果，推荐前往微信 `设置` - `通用` - `字体大小` 设置字体大小为`标准以下`。

![](https://api.minio.1yo.cc/nebuluxe/halosparkpix/image-9e1t.png)

> 在本主题安装完成后，你的显示效果可能跟`效果图示例`有所出入。为了还原`适配设计时`最好的显示效果，请参照说明图或文字说明开启`推荐设置。`

![](https://api.minio.1yo.cc/nebuluxe/halosparkpix/image-ch1j.png)

（完整设置参照图）

*   `顶部导航栏`、`聊天导航栏`、`底部标签页`、`输入框背景`、`所有背景图` 设置为 `主题`。
*   `输入框背景底部` 设置为 `模糊`。
*   `素材加载模式` 设置为 `自定义优先`
*   `启动图` 设置为 `主题`。 其他默认关闭即可。

可选操作：

*   输入框占位文字。如 `Text Message · SMS`
*   语言按钮占位文字。如 `Hold to Talk`

  

*   `全局圆角头像` 启用。（必须!!!!!!!! 所有图标基于圆角模式适配和优化。不启用等于没适配）
*   `私信隐藏双方头像` 启用。

其他根据需要启用。（建议不要启用 `全局布局修改`）  

*   `启用悬浮底栏` 开启。
*   `隐藏标题` 开启。
*   `底栏大小` 设置为 `紧凑`。
*   `边框描边` 亮色设置为 `#c2c2c2`，深色为 `#000000`  
    

  



> 无论你是通过兑换码兑换使用上线版还是使用其他主题加载，都可以使用仓库主题包文件夹内提供的`供替换素材`文件夹替换`红包转账气泡背景封面`、`气泡颜色`、`底栏版本`等。

主题提供多种红包转账气泡封面，持续更新。你可以根据自己的喜好用现有提供的素材替换掉默认的气泡背景。

  

1.  将解压后的主题`02更多红包、转账气泡替换`文件夹打开
2.  拷贝你想要的`红包转账气泡封面背景样式文件夹`内`所有文件`到`Themebox`\-`diy`\-`官方主题`\-`WeChat-iMessage-Themebox`\-`Assets`内覆盖替换本主题默认红包转账气泡背景封面即可。

  
 ![](https://api.minio.1yo.cc/nebuluxe/halosparkpix/%E6%9B%BF%E6%8D%A2%E7%BA%A2%E5%8C%85%E8%BD%AC%E8%B4%A6%E6%B0%94%E6%B3%A1.gif) 

目前`Themebox`不支持于插件内快速检索主题包文件内的供替换`红包转账气泡封面`等素材的功能，需要你：

*   前往`Themebox`主题插件-`主题选项`\-`素材加载模式`选择`自定义优先`。
*   将替换的素材直接丢进`Themebox`\-`diy`\-`iMessage_For_WeChat`内或创建一个子文件夹`Assets`用于收纳。

  

> 素材加载优先级逻辑：  
> 插件会优先加载diy内有的素材，检索同名文件并替换，确保无可替换素材覆盖后加载主题默认素材。（diy优先级>主题默认）
> 
![](https://api.minio.1yo.cc/nebuluxe/halosparkpix/image-drff.png)

在注入有 `微信助手` 动态库的 `IPA` 文件里，可以启用微信助手的工具栏，前往 `我` - `插件` - `微信助手` -启用 `聊天工具栏`，进入 `工具栏配置`，将看到可供添加的功能，选择右上角添加更多功能，这些功能将于 `聊天界面输入栏` 上方显示。

*   启用 `透明背景`。
*   `隐藏标题` 启用。 （ 本主题已对微信助手 `所有` 图标进行 `表意` 设计和优化，建议隐藏标题以在输入栏上方排列更多功能图标。）

你可能会注意到`聊天界面气泡长按功能操作区`的图标有`彩色`和`纯白色`图标混合的情况。  
这并非针对这个区域未做适配，当你仔细和官方操作区图标对比，你会发现纯白色的图标也不同。

![](https://api.minio.1yo.cc/nebuluxe/halosparkpix/image-w7tv.png)

适配过程中`采用过全彩`的图标设计方案，实际界面感受是花里胡哨的，导致聊天界面的彩色要素太多，违背了主题基于系统APP设计的`简约高效`风格的理念。  
最后经过考虑，只保留常用几个需要`谨慎操作`和`快速抓住眼球`的操作为彩色化的方案。  
其他图标已做适配，但保留了纯白以避免太多元素干扰眼球无法快速定位。  

  

> 此部分的设计方案予以`保留`。如果你需要自定义，请自行替换`Assets`内对应素材文件。

部分第三方插件在首页右上角 `加号` 菜单布局流中会插入 `功能槽` 。但由于 `主题插件`或资源引用的限制，该区域部分图标与其他位置图标共用，无法单独设置，会导致部分突兀和不统一问题。

已最大限度保证其他重要区域的图标显示合理，此处突兀已经考虑并降低。

  

> 如果有强迫症无法接受，可以关闭第三方插件在该处占用位置所指向的功能。或者自行 `重新设计` 图标并替换。

故意那么设计的，因为点缀。留透明不好看。

![](https://api.minio.1yo.cc/nebuluxe/halosparkpix/image-y3kh.png)

故意这么设计的。

`主题插件` 设置背景后无法保留原生 ios 开发中`UIKit`库 `UIVisualEffectView` 类的 `实时动态背景模糊` 效果，如果不设置小横线来区分界面板块，整个页面像一张纯黑卡片。没有区分度，反而不好看。（可参考电报深色模式下的顶栏和底栏。）

*   部分设计随着版本更新在替换和优化。
*   如果你对`以往的设计方案`更感兴趣，你可以通过解压后的主题文件夹`01供替换的素材`内找到部分以往设计方案的`保留文件`，根据需要`自行替换`。

1.  随着微信版本的更新图标素材的命名可能会出现变化，需要及时适配新的规则以使素材生效。
2.  该主题根据问题反馈和使用体验进行逐步修复完善，属于查漏补缺式更新和维护。

*   我不是专门的主题制作者，只是一名普通的`Themebox`用户，心血来潮想着完善和优化一个类iMessage的主题。也是在边学、跟大家交流的过程中逐步完善和适配。你就当做这是我某一天将自用的优化的主题在分享出来后被大家支持鼓励推动下持续维护的项目。
*   所以更新和维护速度都无法与“专业人士”相比。
*   虽然不专业，但一定用心维护。用我业余的PS、原型设计技能努力保证此主题的生命周期一直随着微信更新延续下去。直到有一天彻底不能diy微信主题。
*   只会维护这一个主题。

如果你也喜欢该主题，并希望它保持生命活跃。你完全可以加入这个项目，你我皆是这个项目的贡献者。相信有你我的共同努力，它能成为大家在微信主题中类iMessage风中首选的主题。它是无偿的，但它也是炽热的，是你我共同成就的。

* * *